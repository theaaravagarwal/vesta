# Enable only on the loopback-bound compute host immediately before creating a
# Tailscale Serve HTTPS endpoint. The exact login is a Tailscale identity, not
# a device display name. Do not put credentials in this file.
BEHAVIOR_TAILSCALE_AUTH=1
BEHAVIOR_TAILSCALE_ALLOWED_LOGIN=aarav
BEHAVIOR_TAILSCALE_CANONICAL_ORIGIN=https://software-legion-pro-7-16irx9h.mesh.chudmesh.duckdns.org
